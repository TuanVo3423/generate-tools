import {
  AppleIcon,
  ArrowDownIcon,
  ArrowIcon,
  AttachmentIcon,
  BuildingIcon,
  ChevronRight,
  DashboardModalCheckIcon,
  DesignIcon,
  DevelopmentIcon,
  DiscussFillIcon,
  DocumentIcon,
  DotsThreeIcon,
  DragDropFillIcon,
  ExcelIcon,
  EyeClosedIcon,
  EyeIcon,
  GoogleIcon,
  IssueBugIcon,
  IssueCheckIcon,
  IssueIcon,
  IssueWarningIcon,
  LabelCloseCircleIcon,
  LabelCloseIcon,
  LinkIcon,
  LockIcon,
  MoonIcon,
  NotificationFillIcon,
  OpenFolderIcon,
  PdfIcon,
  PlusIcon,
  Priority2Icon,
  PriorityIcon,
  PriorityLowIcon,
  PriorityLowestIcon,
  PriorityMediumIcon,
  PriorityhighIcon,
  QuestionIcon,
  SendIcon,
  ShareIcon,
  SlackIcon,
  StorypointCheckIcon,
  UserPlusIcon,
  WaveFormIcon,
} from '@/icons';
import { Box, Icon } from '@chakra-ui/react';

const Icons = () => {
  return (
    <Box>
      <Icon as={AppleIcon} />
      <Icon as={GoogleIcon} />
      <Icon as={SlackIcon} />
      <Icon as={WaveFormIcon} />
      <Icon as={EyeIcon} />
      <Icon as={EyeClosedIcon} />
      <Icon as={ArrowIcon} />
      <Icon as={BuildingIcon} />
      <Icon as={DesignIcon} />
      <Icon as={DevelopmentIcon} />
      <Icon as={DiscussFillIcon} />
      <Icon as={DragDropFillIcon} />
      <Icon as={MoonIcon} />
      <Icon as={DocumentIcon} />
      <Icon as={PlusIcon} />
      <Icon as={NotificationFillIcon} />
      <Icon as={ChevronRight} />
      <Icon as={PriorityIcon} />
      <Icon as={SendIcon} />
      <Icon as={QuestionIcon} />
      <Icon as={ShareIcon} />
      <Icon as={UserPlusIcon} />
      <Icon as={OpenFolderIcon} />
      <Icon as={ExcelIcon} />
      <Icon as={PdfIcon} />
      <Icon as={LockIcon} />
      <Icon as={IssueIcon} />
      <Icon as={Priority2Icon} />
      <Icon as={DotsThreeIcon} />
      <Icon as={ArrowDownIcon} />
      <Icon as={AttachmentIcon} />
      <Icon as={DashboardModalCheckIcon} />
      <Icon as={LinkIcon} />
      <Icon as={PriorityMediumIcon} />
      <Icon as={PriorityLowIcon} />
      <Icon as={PriorityLowestIcon} />
      <Icon as={PriorityhighIcon} />
      <Icon as={StorypointCheckIcon} />
      <Icon as={LabelCloseIcon} />
      <Icon as={LabelCloseCircleIcon} />
      <Icon as={IssueBugIcon} />
      <Icon as={IssueCheckIcon} />
      <Icon as={IssueWarningIcon} />
    </Box>
  );
};

export default Icons;
